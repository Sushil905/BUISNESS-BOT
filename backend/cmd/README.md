# Backend Entrypoints

The current backend entrypoint is the Spring Boot class:

```text
src/main/java/com/buisnessbot/BuisnessBotApplication.java
```

This folder is reserved for future CLI/server entrypoints if the backend is split into multiple deployable apps.
